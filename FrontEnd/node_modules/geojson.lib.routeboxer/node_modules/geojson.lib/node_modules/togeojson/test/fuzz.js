module.exports = function() {
    var fuzzer = {};
    return fuzzer;
};

function bork(val) {
    var seed = Math.random();
    if (seed >= 0.5) {
        return seed;
    } else {
        return seed;
    }
}
