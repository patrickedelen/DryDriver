#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojson-precision/bin/geojson-precision" "$@"
  ret=$?
else 
  node  "$basedir/../geojson-precision/bin/geojson-precision" "$@"
  ret=$?
fi
exit $ret
