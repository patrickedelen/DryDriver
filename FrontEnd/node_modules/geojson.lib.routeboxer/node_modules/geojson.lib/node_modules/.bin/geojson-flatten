#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojson-flatten/geojson-flatten" "$@"
  ret=$?
else 
  node  "$basedir/../geojson-flatten/geojson-flatten" "$@"
  ret=$?
fi
exit $ret
